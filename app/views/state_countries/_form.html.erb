<%= form_with(model: state_country, local: true) do |form| %>
  <% if state_country.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(state_country.errors.count, "error") %> prohibited this state_country from being saved:</h2>

      <ul>
        <% state_country.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :state %>
    <%= form.text_field :state %>
  </div>

  <div class="field">
    <%= form.label :country %>
    <%= form.text_field :country %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
